# ============================================
# Relay Pulse 生产环境变量配置
# ============================================
# ⚠️ 重要提示：
# 1. 复制此文件为 deploy/relaypulse.env 或 /etc/relay-pulse.env
# 2. 填入真实的 API Key 和数据库密码
# 3. 将 *.env 添加到 .gitignore，避免泄露密钥
# 4. Docker: docker compose --env-file deploy/relaypulse.env up -d
# 5. Systemd: EnvironmentFile=/etc/relay-pulse.env
# ============================================

# ============================================
# 存储配置
# ============================================
# 存储类型：sqlite 或 postgres
MONITOR_STORAGE_TYPE=sqlite

# SQLite 配置（如果使用 SQLite）
# MONITOR_SQLITE_PATH=monitor/monitor.db

# PostgreSQL 配置（如果使用 PostgreSQL）
# MONITOR_POSTGRES_HOST=postgres-service
# MONITOR_POSTGRES_PORT=5432
# MONITOR_POSTGRES_USER=monitor
# MONITOR_POSTGRES_PASSWORD=your_secure_password
# MONITOR_POSTGRES_DATABASE=llm_monitor
# MONITOR_POSTGRES_SSLMODE=require  # 生产环境建议 require 或 verify-full

# ============================================
# API 密钥配置
# ============================================
# 命名规则：MONITOR_<PROVIDER>_<SERVICE>_API_KEY
# - PROVIDER 和 SERVICE 必须大写
# - 短横线 (-) 替换为下划线 (_)
#
# 示例：
# config.yaml 中定义:
#   provider: "88code"
#   service: "cc"
# 则环境变量为: MONITOR_88CODE_CC_API_KEY

# 88code API Keys
MONITOR_88CODE_CC_API_KEY=sk-your-real-key-here
MONITOR_88CODE_CX_API_KEY=sk-your-real-key-here

# DuckCoding API Keys
MONITOR_DUCKCODING_CC_API_KEY=sk-your-real-key-here

# 其他服务商 API Keys
# MONITOR_PROVIDER_SERVICE_API_KEY=sk-your-real-key-here

# ============================================
# 其他配置
# ============================================
# 时区设置（可选，默认 UTC）
TZ=Asia/Shanghai

# Gin 运行模式（可选）
# GIN_MODE=release  # 生产环境建议设置为 release

# CORS 额外允许的来源（可选，仅开发环境使用）
# 生产环境默认只允许 https://relaypulse.top
# 如需允许本地开发，设置此变量（逗号分隔）
# MONITOR_CORS_ORIGINS=http://localhost:5173,http://localhost:3000
